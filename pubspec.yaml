name: waterloo
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  native_pdf_view: ^4.1.0
  file_picker: ^3.0.4
  youtube_plyr_iframe: ^2.0.7

flutter:


